{% extends 'base.html.twig' %}

{% block title %}Modifier le Ticket
{% endblock %}

{% block body %}

	<main class="container my-3">
		<section class="row">
			<div class="col">
                <h1>Modifier le Ticket</h1>
				{{ include('ticket/_form.html.twig', {'button_label': 'Modifier'}) }}

				<a href="{{ path('app_ticket_index') }}" class="btn btn-primary mt-2">Retour à la liste</a>

				{{ include('ticket/_delete_form.html.twig') }}
			</div>
		</section>
	</main>

{% endblock %}

