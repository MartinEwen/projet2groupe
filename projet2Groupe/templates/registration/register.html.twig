{% extends 'base.html.twig' %}

{% block title %}Inscription
{% endblock %}

{% block body %}

	<main class="container my-5" id="shadow-container">
		<section class="row">
			<div class="col">
				<h1 class="mt-3" id="title-gradiant">Inscription</h1>

				{{ form_errors(registrationForm) }}

				{{ form_start(registrationForm) }}
                {{ form_row(registrationForm.lastname, {label: 'Nom'}) }}
                {{ form_row(registrationForm.firstname, {label: 'Prénom'}) }}    
				{{ form_row(registrationForm.email, {label: 'E-mail'}) }}


				{{ form_row(registrationForm.plainPassword, {label: 'Mot de passe'}) }}
				{{ form_row(registrationForm.RGPDConsent) }}

				<button type="submit" class="btn btn-success me-2 mb-3">M'inscrire</button>
                <a href="{{ path('app_main')}}" class="btn btn-info mb-3">Accueil</a>
			</div>
		</section>
	</main>
	
	{{ form_end(registrationForm) }}
{% endblock %}

