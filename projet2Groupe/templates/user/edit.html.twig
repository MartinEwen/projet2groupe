{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<main class="container my-3">
		<section class="row">
			<div class="col">
				<h1>Edit User</h1>

				{{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}

				<a href="{{ path('app_user_index') }}" class="btn btn-primary me-2">Retour à la liste</a>
                <a href="{{ path('admin_index') }}" class="btn btn-primary">Retour Admin</a>

				{{ include('user/_delete_form.html.twig') }}
			</div>
		</section>
	</main>
{% endblock %}

